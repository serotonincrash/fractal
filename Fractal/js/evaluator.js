import * as mathjs from 'mathjs';

class JSMathEvaluator {
    const parser = math.parser();
    const answer: Number;
    
    static setVar(variable, value) {
        parser
    }
    static evaluate(expression) {
        let result = parser.evaluate(expression, );
        answer = result;
        
        return result;
    }
    
}
